# freesound-org-cli

Read README.md, then relevant docs. Keep command handlers thin and machine output stable. Use TypeScript, citty, native fetch and Node-compatible APIs; verify Bun separately. Never store credentials or real user media in this repository. Run npm run check before publishing changes. Tests must use mocks or synthetic audio, not paid generation.
